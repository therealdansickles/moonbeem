import { MintSaleTransaction } from './mint-sale-transaction.entity';

describe('MintSaleTransaction', () => {
    it('should be defined', () => {
        expect(new MintSaleTransaction()).toBeDefined();
    });
});
